#include<stdio.h>
int main(){

    int num;
    printf("Enter your Number - ");
    scanf("%d",&num);


    printf("Convert into Hexadecimal System - %x\n",num);
    printf("Convert into Octal System - %o\n",num);

    // if we change the format specifier('%d to %o or %x') technology will change the data type.

}
