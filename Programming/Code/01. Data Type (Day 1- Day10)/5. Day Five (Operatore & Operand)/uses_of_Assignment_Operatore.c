#include<stdio.h>

int main(){

    int volume, length = 7, width = 10;
    volume = length * width;

    printf("%d",volume);

    getch();
    return 0;

    // minus equal to Operator

    a += b // == (a = a+b)

    // -=, *=, /=, %=;



}
